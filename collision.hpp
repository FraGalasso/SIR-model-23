#define COLLISION_HPP
#ifndef COLLISION_HPP

#include "persona.hpp"

/* Popolazione void collision() {
  for (int i = 0; i < size(); ++i) {
    for (int j != i; j < size(); ++j) {
      int r = v[i].GetX();
      int px = v[i].GetPx();
      int py = v[i].GetPy();
      int r_ = v[j].GetX();
      int px_ = v[j].GetPx();
      int py_ = v[j].GetPy();
      if (r == r_) {
        px = -px;
        px_ = -px_;
        py = -py;
        py_ = -py;
      }
    }
  }
} */

#endif